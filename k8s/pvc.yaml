apiVersion: v1
kind: PersistentVolume
metadata:
  name: sales-service-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /tmp/subiv-sales-service-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sales-service-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
